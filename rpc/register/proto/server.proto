syntax = "proto3";

package moshi;

message EchoArguments {
    string msg = 1;
}

message EchoResult {
    string msg = 1;
}

service RpcServer {
    rpc echo(EchoArguments) returns (EchoResult); // 返回用户的ip+port+msg
}

